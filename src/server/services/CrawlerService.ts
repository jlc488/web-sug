import * as cheerio from 'cheerio'
import * as urlParse from 'url-parse'
import * as request from 'request'

export class CrawlerService {
  public findSubURLs(mainURL: string): Promise<any> {
    return new Promise((resolve, reject) => {})
  }
}
